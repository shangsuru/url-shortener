export default function Data() {
  return (
    <div>
      This will later contain a graph with the number of clicks (per hour, per
      month, per year), a world map showing from where the clicker came from,
      and possibly other stats like top devices (desktop, smartphone, tablet),
      top browsers (chrome, chrome mobile, firefox, edge others) and top
      platforms (mac, windows, android, linux others) or top languages and most
      popular hours in UTC
    </div>
  );
}
